package codelook.jpa.listing;

import org.springframework.modulith.ApplicationModule;

@ApplicationModule
public class ListingModule {
    // This annotation declares the module and its dependencies (if any)
}
